NODE_ENV=
PORT=3000

# Direct PostgreSQL connection string from Neon (used by Prisma Migrate CLI)
DATABASE_URL=

# Optional: Prisma Accelerate proxy URL (used for connection pooling in production)
# ACCELERATE_URL=

JWT_SECRET=
JWT_EXPIRES_IN=90d